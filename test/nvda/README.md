# 真 NVDA 對照（手動驗證工具）

我們的「NVDA 報讀預覽／朗讀順序」是一套**模擬**——用詞逐字校對自 NVDA 官方
`zh_TW.po`，但「何時念什麼、順序怎麼組」是我們自己的模型。這個工具用 **Guidepup
驅動真正的 NVDA**，擷取它實際念出的每一句，與我們的 `__rampA11yReadingOrder`
輸出並排比對，用來校準模擬的忠實度。

## 安裝與執行

```sh
npm install
npx @guidepup/setup                 # 裝 Guidepup 專用 NVDA（含 NVDA Remote 附加元件）
npm run test:nvda                   # 對照 test/nvda-fixture.html
npm run test:nvda -- reading-order-fixture.html   # 換頁面
```

## ⚠ 執行注意（需互動桌面，不進 CI）

執行期間 NVDA 會出聲朗讀、桌面視窗會被最小化，屬預期行為。焦點處理已**全自動**
（細節見 compare.js 檔頭：NVDA 啟動會觸發 Windows 彈出「設定」、Guidepup 的語音檢視器
置頂攔截點擊、CDP 合成鍵盤 NVDA 聽不到——都已在工具內處理）。

## ✅ 已完成：真 NVDA 逐控制項對照（2026-07-11）

真 NVDA 逐一 Tab 走過 `nvda-fixture.html`，與模擬並排比對——**名稱、角色、值、狀態、
描述、地標的用詞與組合全部吻合**：

| 控制項 | 真 NVDA 實際念出 | 我們的模擬 | 判定 |
|---|---|---|---|
| 城市下拉 | 城市, 下拉式方塊, **台中**, 折疊 | 城市 下拉式方塊 台中 | ✅ 值正確 |
| 帳號欄 | 帳號, 編輯區, **請輸入 6 到 12 個英數字**, 已選取 user 123 | 帳號 編輯區 user123（請輸入…） | ✅ 描述會唸 |
| 更多按鈕 | 更多, 功能表按鈕, **折疊, 子功能表** | 更多 功能表按鈕 折疊 子功能表 | ✅ 狀態逐字命中 |
| 備註 | 備註, 編輯區, **多行**, 空白 | 備註 編輯區 多行 | ✅ |
| 首頁連結 | **主要選單, 導覽區 地標**, 首頁, 連結 | 〔主要選單 導覽區〕→ 首頁 連結 | ✅ 地標邊界獲證實 |
| 興趣清單 | 興趣(可複選), 清單 | 興趣（可複選）清單 有 3 項 | ✅（項目數為瀏覽模式播報） |

**驗證同時抓出並已修正的兩個落差**（正是對照的價值）：
- `edit` 角色：我們原寫「編輯」→ 真 NVDA 念「**編輯區**」（官方 po 亦證實），已修正
- 帶 `aria-haspopup` 的按鈕：真 NVDA 念「**功能表按鈕**」而非「按鈕」，已修正

已知的模擬簡化（非錯誤）：Tab 聚焦時 NVDA 會補念「已選取」「空白」等即時輸入狀態、
清單項目數只在瀏覽模式播報——這些依情境浮動的細節，模擬以靜態快照呈現。
